[auth]
jwt_secret = "testsecret"

[server]
host = "localhost"
port = 3000
logging = true
log_level = "log_level"
log_format = "log_format"

[database]
host = "localhost"
port = 1337
database = "pcast"
user = "pcast"
password = "pcast"
max_connections = 10
max_idle_connections = 5
max_lifetime = "5m"
logging = false
time_zone = "Europe/Berlin"
